# API — URL de base (avec /api/v1). En dev, via le tunnel SSH vers le VPS.
VITE_API_BASE_URL=http://localhost:8080/api/v1

# Langue par défaut de l'interface (en | fr | ar)
VITE_DEFAULT_LOCALE=fr

# NB : pas de « stubs » d'API. Décision V1 (03/08) : on ne SIMULE jamais un
# endpoint absent (contrat d'API §4). Les bouchons restent aux tests unitaires.
