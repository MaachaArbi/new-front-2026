# Dev : le front (localhost:5180) parle à l'API via le tunnel SSH (localhost:8080).
# localhost = contexte sécurisé → le cookie httpOnly `ostravel_refresh` (Secure) est gardé.
VITE_API_BASE_URL=http://localhost:8080/api/v1
