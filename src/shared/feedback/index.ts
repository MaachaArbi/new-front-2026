/**
 * Composants de retour visuel de chargement (ADR-F20.3 / F20.4). Hors
 * `src/shared/ui/` (réservé aux prélèvements ReUI) : primitives maison.
 */

export {
  Skeleton,
  SkeletonText,
  SkeletonRow,
  SkeletonCard,
  SkeletonField,
} from './skeleton'
export { StaleContent } from './stale-content'
